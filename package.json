{
  "name": "cancellation_booker",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "tsc": "npx -p typescript tsc",
    "bundle": "browserify src/index.js src/standalone.js -o out/bundle.js",
    "build": "npx tsc && npx bundle",
    "search": "node old/standalone.js"
  },
  "dependencies": {
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "node-fetch": "^3.2.5",
    "@babel/preset-env": "^7.18.2",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "typescript": "^4.7.3"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "@babel/preset-env"
          ]
        }
      ]
    ]
  }
}
