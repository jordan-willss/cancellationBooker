{
  "name": "cancellation_booker",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "bundle": "browserify src/interface.js src/runner.js -o out/bundle.js",
    "search": "node src/standalone.js"
  },
  "dependencies": {
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.18.2",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "@babel/preset-env"
          ]
        }
      ]
    ]
  }
}
